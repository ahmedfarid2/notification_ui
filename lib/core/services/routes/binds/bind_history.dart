import 'package:flutter_modular/flutter_modular.dart';

class BindHistory {
  static void binds(Injector i) {
    // i.add<BoxHistory>(BoxHistory.new);

    // /// [Data Sources]
    // i.add<LocalHistory>(LocalHistoryImp.new);

    // /// [Repo]
    // i.add<RepoHistory>(RepoHistoryImp.new);

    // /// [UseCases]
    // i.add(UseCaseSetHistoryItem.new);

    // /// [State Management]
    // i.addLazySingleton<HistoryCubit>(HistoryCubit.new);
  }
}
